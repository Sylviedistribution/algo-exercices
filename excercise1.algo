ALGORITHM leapYearChecker
VAR
    year: INTEGER;

BEGIN
    WRITE("Give a year: ");
    READ(year);
    IF ((year%4=0 AND year%100!=0) OR year%400=0 ) THEN
        WRITE("It's a leap year.")
    ELSE
        WRITE("It's a leap year.")
    END_IF
END